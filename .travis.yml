language: go

before_install:
        - .travis/gofmt.sh
        - .travis/goimports.sh
        - .travis/gometalinter.sh
        - go get github.com/mattn/goveralls

install: .travis/install.sh

script:
        - LOG_UPTO="LOG_ALERT" $HOME/gopath/bin/goveralls -service=travis-ci

